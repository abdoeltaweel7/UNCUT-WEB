import Link from 'next/link'; import {notFound} from 'next/navigation'; import {projects} from '@/data/projects'; import {ProjectMedia} from '@/components/work/ProjectMedia';
export default async function Project({params}:{params:Promise<{id:string}>}){const {id}=await params;const project=projects.find(p=>p.id===id);if(!project)return notFound();return <main className="project"><div className="project-cover" style={{backgroundImage:`linear-gradient(0deg,#050505,transparent 70%),url(${project.coverImage})`}}><div className="project-cover-label"><span>{project.number} / {project.tag}</span><span>UNCUTS PRODUCTION</span></div></div><section><span className="eyebrow">PROJECT {project.number} — {project.year}</span><h1>{project.title}</h1><div className="project-meta"><span>CLIENT<br/><b>{project.client}</b></span><span>CATEGORY<br/><b>{project.category}</b></span><span>FORMAT<br/><b>{project.mediaType === 'video' ? 'FILM' : 'STILLS'}</b></span></div><p className="project-description">{project.description}</p><p className="arabic-copy project-arabic">{project.arabicDescription}</p><ProjectMedia project={project}/><Link href="/work" className="text-link">العودة للمعرض <span>↗</span></Link></section></main>}
