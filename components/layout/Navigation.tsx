'use client';
import Link from 'next/link'; import {usePathname} from 'next/navigation'; import {useState} from 'react'; import {navigation} from '@/data/siteConfig';
export function Navigation(){const path=usePathname();const [open,setOpen]=useState(false);return <header className="nav"><Link className="brand" href="/" aria-label="Uncuts Production home"><img className="brand-logo" src="/uncuts-logo.jpeg" alt="Uncuts Production" /></Link><div className="nav-meta"><span>SAUDI ARABIA / 2026</span><span className="live-dot" aria-hidden="true"/> <span>THE SHOW IS LIVE</span></div><button className="menu-toggle" onClick={()=>setOpen(!open)} aria-expanded={open} aria-controls="main-navigation" aria-label={open?'Close menu':'Open menu'}><span/ ><span/ ><span/ ></button><nav id="main-navigation" className={open?'open':''}>{navigation.map(([num,name,href])=><Link onClick={()=>setOpen(false)} className={path===href?'current':''} href={href} key={href}><small>{num}</small>{name}</Link>)}</nav></header>}
